#include <stdio.h>

int main()
{
    printf("Enter your marks  (out of 100):");
    int m=0;
    scanf("%d",&m);
    char grade[8]="A\0B\0C\0D\0F";
    
    if (m>=85 & m<=100)
    {
        printf("Grade %s",&grade[0]);
    }
    else if (m>=70 & m<=84)
    {
        printf("Grade %s",&grade[2]);
    }
    else if (m>=55 & m<=69)
    {
        printf("Grade %s",&grade[4]);
    }
    else if (m>=40 & m<=54)
    {
        printf("Grade %s",&grade[6]);
    }
    else if (m<=40)
    {
        printf("Grade %s",&grade[8]);
    }

    else
    {
        printf("Invalid value");
    }


}
